@{
    ViewBag.Title = "About";
    var db = new oxinhem.Models.MyAppdbContext();
    var about = new MyModels.About();
    about = db.abouts.OrderByDescending(m => m.aboutID).FirstOrDefault();
    var routeValues = this.ViewContext.RouteData.Values;
}

<section class="hero-wrap hero-wrap-2" style="background-image: url('/CoContent/images/about us.jpg');" data-stellar-background-ratio="0.5">
    <div class="overlay"></div>
    <div class="container">
        <div class="row no-gutters slider-text align-items-end">
            <div class="col-md-9 ftco-animate pb-5">
                <p class="breadcrumbs mb-2"><span class="mr-2"><a href="/Home">@Resources.lang.Menu_Home <i class="fa fa-chevron-right"></i></a></span> <span>@Resources.lang.Menu_About <i class="fa fa-chevron-right"></i></span></p>
                <h1 class="mb-0 bread">@Resources.lang.Menu_About</h1>
            </div>
        </div>
    </div>
</section>

<section class="ftco-section ftco-no-pt ftco-no-pb">
    <div class="container">
        @if (routeValues["culture"].ToString() == "en")
        {
            @Html.Raw(about.Body)
        }
        else
        {
            @Html.Raw(about.BodySe)
        }
    </div>
</section>